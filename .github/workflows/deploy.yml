name: deploy and configure
on: [push]
jobs:
  local:
    runs-on: Linux
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run it
        uses: ./
        with:
          playbook: playbook.yml
          directory: test
          options: --inventory host
